public enum EntityType
{
    Ship,
    Asteriod,
    Station
}
public enum StationType
{
    Hanger
}

public enum AimType
{
    Forward,
    Backwards,
    MouseAim
}

public enum WeaponType 
{
    Projectile,
    Laser,
    Beam,
    Mine
}